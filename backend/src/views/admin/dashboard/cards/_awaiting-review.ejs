<% if (s.awaitingReview.length === 0) { %>
<div class="stat-empty">Žiadne články nečakajú na review.</div>
<% } else { %>
<ul class="stat-list">
  <% s.awaitingReview.forEach(a => { %>
  <li>
    <a href="/admin/articles/<%= a.id %>/edit" class="li-t"><%= a.title %></a>
    <span class="li-m"><%= a.author || '—' %> · <%= new Date(a.updated_at).toLocaleDateString('sk-SK') %></span>
  </li>
  <% }) %>
</ul>
<% } %>
