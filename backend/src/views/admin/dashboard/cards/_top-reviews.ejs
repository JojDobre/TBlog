<% if (s.topReviews.length === 0) { %>
<div class="stat-empty">Žiadne hodnotené recenzie.</div>
<% } else { %>
<ol class="stat-rank">
  <% s.topReviews.forEach((a, i) => { %>
  <li><span class="rk"><%= i+1 %></span><a href="/admin/articles/<%= a.id %>/edit" class="rk-t"><%= a.title %></a><span class="rk-v" style="color:var(--accent)"><%= a.score %>/10</span></li>
  <% }) %>
</ol>
<% } %>
